spring:
  application:
    name: issue

server:
  port: 8080
  http2:
    enabled: true
  compression:
    enabled: false
  tomcat:
    threads:
      max: 400
  jetty:
    threads:
      max: 400

logging:
  level:
    root: warn
